# iPhone 幻梦上报快捷指令 · 完整配置

## 一、当前已跑通的结构（3 动作）

```
① 获取 URL 的内容（GET）
   URL = https://gh-proxy.com/https://raw.githubusercontent.com/wss542/huanmeng-kook-bot/master/phone_tunnel_url.txt

② 从输入获取文本
   （自动接 ① 的输出，把"多信息文本"转成纯文本）

③ 获取 URL 的内容（POST）
   URL 栏 = [② 的输出] + 手打 /phone?key=HMphone2026_kQ2
   方法   = POST
   请求体 = JSON（见下方字段表）
```

> 关键坑回顾：① 的输出是"多信息文本"，不能直接填进 ③ 的 URL 栏，否则报
> `无法将多信息文本转换为 URL`。必须先用 ②「从输入获取文本」转纯文本。

---

## 二、把写死的值换成真实变量（⚠️ 关键：避开弯引号 bug）

### 你已经踩的坑（为什么之前"与 bot 不符"）
我抓了 VM 收到的真实 JSON，问题出在 **iOS 快捷指令插入变量时会自动加智能弯引号（`""` 而不是 `"`），并且类型包装不对**：

```python
# ❌ 你实际传上去的（错误样本）
{
  'battery': {'level': '“73”', ...},        # 字符串 + 弯引号 → VM 解析成 None，卡片不显电量
  'network': {'WiFi': '“广禾瑞佳-5G”'},      # 键名是 WiFi（大写）→ VM 只读 wifi（小写），读不到
  'music':   {'title': '“”', 'artist': '“”'} # 弯引号空串
}
```

VM 端 `_norm_battery` 用 `float(lvl)` 转电量，`float('“73”')` 会抛错 → 电量变 `None`。
VM 端 `_norm_network` 只读 `n.get("wifi")`，你传的是 `WiFi` → 读不到 WiFi 名。

### 修法 1：键名必须小写 `wifi`（不是 `WiFi`）
编辑 ③ 的 JSON 请求体，`network` 里的键写成 **`wifi`**：
```
"network": {"wifi": "未知", "cellular": true}
```
（iOS 拿不到 WiFi 名，先固定 `"未知"`；cellular 固定 `true`）

### 修法 2：电量/充电/歌曲变量必须先"洗掉弯引号"
iOS 插入的变量带弯引号，必须加一道「**替换文本**」动作把 `“` `”` 删掉，否则 VM 收的是字符串不是数字。

**做法（针对 battery.level 这个变量）：**
1. 在 ② 之后、③ 之前，加动作 **「替换文本」**（Search "Replace Text"）
   - 这个动作的"输入"= 你的「电池电量」变量
   - 把 `“` 替换为 ``（空）
   - 再把 `”` 替换为 ``（空）
   - 输出设为变量 `电量干净`
2. ③ 的 JSON 里 `battery.level` 用 `电量干净` 变量（现在是纯数字文本，VM 能 `float()` 转成 73）

> 简单判断：如果电量字段你直接插「电池电量」变量、且 JSON 值显示成 `"73"`（带引号），就一定要走「替换文本」洗掉。显示成 `73`（不带引号、数字类型）则不用。

### 变量对照表（改 ③ 的 JSON 请求体）

| 键路径 | 改成 | 说明 |
|---|---|---|
| `battery.level` | `电量干净` 变量（经替换文本洗过） | 数字，VM 自动 ×100 兼容 |
| `battery.charging` | 「是否正在充电」变量 | 布尔 |
| `battery.low_power` | `false` | 固定 |
| `music.title` | 「当前歌曲」变量（可经替换文本洗弯引号） | 没播放则为空 |
| `music.artist` | `""` | iOS 拿不到歌手，固定空串 |
| `music.app` | `"Music"` | 注意大写 M，别写 `music` |
| `music.playing` | `false` | 简单做法固定 false |
| `network.wifi` | `"未知"` | 小写键名 + 固定值 |
| `network.cellular` | `true` | 固定 |
| `device` | `"iPhone"` | 固定 |
| `app` | `"unknown"` | 固定 |

### 怎么插变量
1. 点 JSON 里那个值
2. 删掉原值
3. 在弹出的变量列表里选（「电池电量」/「是否正在充电」/「当前歌曲」等）
4. 带弯引号的务必先过「替换文本」

### 需要提前获取这些变量吗？
不用额外加动作——「电池电量」「是否正在充电」「当前歌曲」是快捷指令**内置魔法变量**，直接在 JSON 值里插。列表里没有才单独加「获取设备详情 / 获取当前播放」。

---

## 三、设自动化触发（不用手动跑）

打开「快捷指令」→ 底部「自动化」标签 → 右上角 `+`：

### 方案 A：电池变化触发（推荐，命中率高）
1. 选「电池电量」
2. 条件：比如「电池电量 下降/上升」或「充电器 连接/断开」
3. 动作：运行「幻梦上报」快捷指令
4. 关掉「运行前询问」（否则每次弹窗）

### 方案 B：音乐播放触发
1. 选「播放」或「App」→ 选「音乐」
2. 运行「幻梦上报」

### 方案 C：App 打开触发（⭐ 最推荐，近实时）
每次打开常用 App 就上报一次，覆盖面比电池/音乐广得多，基本做到"你拿起手机看一眼，/sys 就是最新的"。
1. 选「App」→ 选你常用的一堆 App（微信、Safari、相机、设置、游戏等，可多选）
2. 条件：「已打开」（默认就是 App 启动时触发）
3. 动作：运行「幻梦上报」快捷指令
4. 关掉「运行前询问」（否则每次弹窗烦死）

> 实测：多选 8-10 个常用 App，日常 1-2 分钟就会触发一次上报。比定时更准、比电池更密。

### 方案 D：定时兜底（抵消隧道偶发故障）
1. 选「时间」→ 每天 / 或「特定时间」
2. 设每天多个时间点（如 9:00 / 12:00 / 18:00 / 22:00），或选「特定时间」加多个时间点
3. 运行「幻梦上报」
4. 关掉「运行前询问」

> 建议 **C + A 组合**：App 打开触发（高密度）+ 电池变化（充电/拔线也报），基本覆盖日常。
> 隧道偶发丢失可接受——下次触发会自动补上。

### 关于"发 /sys 时自动读一次手机"
**架构限制**：KOOK bot 在 VM 上，无法反向唤醒 iPhone 的快捷指令（iPhone 没有公网入口，只能主动 POST 给 VM）。所以 `/sys` 做不到"真·拉取手机"。
**解决方案（已落地）**：
- bot 侧已增强 `/sys` 卡片：每个设备区块显示「上报于 X 分钟前」，超过 30 分钟没上报的 iPhone 卡片**整体标红**并提示「⚠数据可能过期」。
- 你只要保持 iPhone 自动化（方案 C/A）开着，`/sys` 看到的手机卡就基本是近况。
- 想要 100% 实时：发 `/sys` 前，先在 iPhone 上手动点一下「幻梦上报」快捷指令（或靠 App 打开自动触发），再发 `/sys`。

---

## 四、验证

1. 手动跑一次快捷指令 → 应返回 `{"ts":...,"ok":true}`
2. KOOK 里 `@茂密 /sys` → 应看到「iPhone」设备区块，标题下方显示「上报于 X 秒/分钟前」
3. 打开一个已配自动化的 App（如微信）→ 等几秒 → 再发 `/sys` → 应看到 iPhone「上报于 0 分钟前」（刚报过）
4. 若 iPhone 很久没动 → `/sys` 里 iPhone 卡片**标红** + 「⚠数据可能过期」，提示你去触发一次上报

---

## 五、故障排查

| 现象 | 原因 | 解决 |
|---|---|---|
| `无法将多信息文本转换为 URL` | ① 输出没经「从输入获取文本」直接进 ③ URL 栏 | 确认 ② 存在且 ③ URL 栏引用的是 ② |
| 返回 404 | 隧道轮换，发现入口还没同步 | 等 1-2 分钟重跑（VM 自动同步） |
| 返回 503 | 隧道临时不可用 | 等几分钟重跑；cloudflared 隧道已替换 localhost.run，稳定性大幅提升 |
| `ok:true` 但 `/sys` 不显示 iPhone | 数据 stale 或 device 字段不对 | 确认 JSON 里 `device` 是 `iPhone`；数据 30 分钟内算新鲜 |
| `/sys` iPhone 卡片标红「⚠数据可能过期」 | iPhone 超过 30 分钟没上报（锁屏/没开自动化） | 打开一个配了自动化的 App 触发上报，或手动跑快捷指令 |
| `/sys` 显示电量空/None | 电量变量带弯引号 `“73”`，VM `float()` 失败 | 见第二节修法 2：电量变量先过「替换文本」洗掉弯引号 |
| `/sys` 显示 WiFi 名为空 | `network` 键名写成 `WiFi`（大写），VM 只读 `wifi` | 键名改小写 `wifi` |
| 音乐显示 `music`(小写 app) 或空歌名 | app 写成 `music`、title 带弯引号 | app 写 `Music`；title 过替换文本洗弯引号 |
